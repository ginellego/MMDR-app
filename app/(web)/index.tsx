import { Text, View } from 'react-native';

export default function WebHome() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>This app is not supported on web.</Text>
    </View>
  );
}
